<% if(typeof movies !=='undefined') {%>
    <% movies.forEach(movie => { %>
        <div class="movie-card">
            <h2><%= movie.title %></h2>
            <p><%= movie.synopsis %></p>
        </div>
    <% }); %>
<% } else { %>
    <p>No movies found.</p>
<% } %>